# 结业测验规则（对话内逐题）

用户学完某一课后，**必须在 Cursor 对话中**进行结业检验。本规则自 2026-08-22 起生效，后续每一课均适用。

## 何时触发

- 用户说「第 N 课学完了」「考一下我」「检验学习情况」等；
- 或单次会话中用户刚完成新课学习，Agent 主动询问是否开始结业测（用户同意后再出）。

## 怎么做（核心）

1. **一题一题问**：每次只出 **1 道题**；用户答完并看到解析后，再出下一题。
2. **不要**一次性列出整卷题目。
3. **不要**为此单独新建 HTML 结业测页面或修改课程页（除非用户**明确要求**改页面）。
4. 测验在 **Cursor 对话**里完成。

## 选择题怎么出

- **优先**使用 Cursor **结构化选择题**（Agent 的 AskQuestion 工具），让用户 **鼠标点选** 选项。
- 用户也可直接回复 **A / B / C / D**；两种形式均有效。
- 选项**长度尽量一致**，避免通过格式泄题（与课内 `quiz.js` 规则相同）。

## 单题流程

```
出题（1 题）→ 等待作答 → 立刻判对错 + 简短解析 → 下一题
```

- 解析：先给对错，再 1–3 句说明依据；错题可补一句口诀。
- 进度提示：如「第 3 / 7 题 · 目前 2 对 1 错」。

## 题量与选题

| 项 | 要求 |
|----|------|
| 题量 | 每课 **5–7 题** |
| 覆盖 | 本课核心考点 + 课内测验已出现的高频点 |
| 复习 | 从 `learning-records/` 抽 1–2 题复习**历史易错点**（如密钥方向、CRL、DMZ 等） |
| 难度 | 单选为主；与软考上午卷风格一致；必要时 1 道下午卷风格简答（仍逐题） |

## 结业汇总

全部做完后给出：

- 得分（如 **6 / 7**）与错题列表；
- 每道错题对应口诀或速查锚点（如 `reference/mnemonics.html#lesson-05`）；
- 是否建议进入下一课，或先补哪一块。

## 记录（必做）

1. 追加 `learning-records/000N-<dash-case>.md`：分数、错题、易错 implication。
2. 更新 `NOTES.md` 教学记录：该课「已学完 + 结业测分数」。
3. 若 AGENTS.md「当前进度」过时，同步更新下一课入口。

## 示例话术（Agent）

- 开始：「第 5 课结业测，共 7 题，一题一题来。」
- 中间：「第 2 题不对，正确答案是 C……下面第 3 题：」
- 结束：「结业测 6/7，唯一错题是……可以进第 6 课。」

## 与课内 HTML 测验的关系

- **课内 HTML**（`lessons/*.html` 里的 `quiz.js`）：学习**过程中**自学、即时反馈。
- **对话结业测**：用户宣布**学完后**由 Agent 主持，检验存储强度；二者都要，不互相替代。

## 明确不做

- 不默认创建 `reviews/` 目录或独立结业测 HTML；
- 不在对话里一次甩 10 道题让用户填表；
- 不跳过结业测直接进入下一课（除非用户明确说「先不测」）。
